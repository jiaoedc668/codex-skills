# 版本记录

版本唯一值为SKILL.md的metadata.version；已发布标签不移动。发布流程见references/release-management.md。

## 1.4.0 — 2026-09-10

兼容增强剧情创作能力。v1.3.0 已将自由创作前置，但单句口语要求仍容易被“把道理说全、把下一步交代完”的惯性覆盖；题材差异也未充分约束批量对话推进。此次集中修正这些创作机制，不重写架构。

- 高优先级明确发哥帮普通人想明白复杂现实问题，以事实、边界、成本与选择建立信任；不滑向情感导师或标准答案机器。
- Creative Review 增加去作者感、去导师感核心判废、作者解释删除测试和教程检测；一次回应一个主要动作，方法只展开到当前必要的一步。
- 70%大白话、30%传播句只作创作启发；通常保留一个主要传播句，抑制排比、漂亮话接力和重复升华，不作比例统计。
- Raw Draft 主动寻找冲突方有现实动机的情绪原话，开场1～2个来回尽快呈现人物与异常行为或损失；保留必要背景，不机械悬念。
- 增加模型级 batch Creative Review，比较发动、信息揭示、判断时机、认同程度、停止位置与收尾；不设叙事配额，也不把反问留白变成统一模板。
- 结尾双审事件内生与不过度收口，允许真实顾虑、不认同和未决选择；删除正文观点的文艺重复，回收具体故事元素。
- 标题压缩到一个可兑现冲突，约8～14字仅作参考；同步标题限定任务，不新增字符数门。
- 新增 [现役创作校准](references/creative-quality-calibration.md) 六组通用虚构短例，四个创作入口接入；旧对白正文完整迁入 legacy，原路径保留退役说明与现役入口。
- 退役过时的1.0.0→1.1.0手动改版指令，SKILL-VERSION-CHANGE.txt仅作当前发布说明；发布流程补充模型级创作回归及非业务边界。

### 兼容与验证边界

保留 Raw Draft → Creative Review → schema、schema v6、人物事实与安全边界、研究门、反馈台账和 INTEGRITY PASS/FAIL；Python、schema合同、人物模板、历史反馈、公开快照及其他Skill均未修改。未新增评分器、黑名单、质量字段或机器创作判定。回归完整稿不进入仓库，不写候选或用户反馈。

完整 unittest 226项通过，scripts compileall及git diff --check通过。仓库Markdown相对文件链接有效，Junction入口哈希一致；旧样例正文与迁移前逐行一致。初始化行为由现有测试验证，本轮无需人物迁移。官方quick_validate因缺少PyYAML未启动，不冒称通过、不新增依赖。仓库没有PROGRESS.md，迭代记录集中于本条，不改业务项目状态。

### 模型级创作回归

独立上下文以v1.3.0生成三题材基线，再以现役规则新写三篇并审读。基线三篇均由诉苦、追问进入，并由当事人宣布下一步收尾；职场建议存在连续展开，家庭末段偏离养老退路。新版首稿审读仍发现完整分析、立即认同的惯性，因此删除对应段落；主审又发现无事实支持的迁怒指责及刻意秘密转折，补充信息自然引出与判断证据校准后重写复验。

最终家庭稿集中于养老积蓄难以补回，保留父亲担忧；职场稿从拒绝带人进入，回到迟迟未获回答的工资问题与留下的代价；欠款稿由欠款人主动争辩，暴露其替债主决定等待的做法，未安排分期教程。开场均迅速交代具体冲突，情绪句有现实动机，传播表达服务本篇，结尾未统一认同或金句收口。主审进一步压缩家庭标题，纠正欠款标题暗示已还款的超额承诺，减少家庭稿连续追问。

这证明本轮规则能引导发现并修订问题，不证明首次生成已稳定达到人工精修质量；家庭追问密度与职场口语仍需真实使用反馈校准。未做机器质量评分、实际朗读、拍摄或用户可拍验收。完整回归稿只在本次内部审读中使用，未提交；本轮生成的Python缓存已清理，可由测试重建。

## 1.3.0 — 2026-09-10

- 调整创作优先级，停留感、真人感和冲突质量提前；人物与事实安全仍为硬约束。
- 新增 Raw Draft 自由创作与 Creative Review，选题池、brief、蓝图和schema后移；schema描述已成立的故事，不反向生成故事。
- 普通常青虚构剧情取消“3条10万赞原作”和跨期高互动证据的阻断门；原作研究成为可选表达增强。
- 保留热点、事实和明确研究任务的严格来源核验；所有填写的高赞样本仍须真实、单视频达标且内容可读，禁止凑数或拼接原作。
- 强化具体行为冲突及符合人物身份的情绪触发句；发哥可以追问、被反驳、改口，减少全知型与老师型表达，剧情原则上只保留一次完整观点输出。
- 增加前3秒具体行为 Hook 检查、演员读得出口测试及真人优先语义审读，不以自动评分代替创作。
- 强化事件内生型结尾，回收当前动作、损失与选择，减少万能人生金句。
- 强化正向反馈机制沉淀与keep实际使用；原话与系统提炼分开，减少只累计avoid造成的保守化。

### 兼容与实现

候选仍schema v6，历史v5/v6继续读取，不迁移人物模型、旧候选、正式稿或业务台账。研究校验最小调整于select_topics.py及dual_format_contract.py：viral_expression_samples仍为数组但不设最少条数；v6增加可选research_required布尔标记，true须sources或fact_sources，current_issue仍要求七天内热度来源。旧v5研究分支保留。

反馈仍schema2，history_manager.py兼容新增candidate_feedback事件，复用preserve/avoid并投影keep，不计可拍验收、不覆盖旧字节。新版可读旧台账；含新事件的台账不能直接退回旧版读取器。未增加Raw Draft程序对象、effective_mechanisms或质量评分字段。

### 验证

研究门定向9项、正向反馈定向4项及现有反馈/历史验收测试通过；完整unittest共226项通过，scripts compileall及git diff --check通过。相对链接和安装Junction的入口哈希一致性检查通过。官方quick_validate因本机缺少PyYAML未能启动，未新增依赖，不冒称该检查通过。独立规则行为检查确认旧版常青创作被阻断，新版先自由创作；这不是用户可拍验收，也不证明传播效果或来源在线真实性。历史版本记录中的旧研究门仅描述当时行为，以1.3.0现役规则为准。

## 1.2.0 — 2026-09-10

兼容新增本地业务状态到普通 ChatGPT Chat 公开快照的同步器，不改变业务人物、反馈、选题和共享台账的真相源，也不改变 schema v6、人物 schema 3、本地反馈状态机或 Word 授权门。

### 本次优化

- 新增 `scripts/sync_public_context.py`，优先复用 `history_manager.py` 的读取和反馈汇总能力。
- 新增固定白名单投影，剥离 `user_quote`、内部路径、完整稿件、研究正文、认证信息、手机号和邮箱等敏感信息。
- 同步器只读取本地业务状态，四个公开快照全部构造并通过安全检查后才写入；读取或安全检查失败时保留已有快照。
- 增加源文件字节不变、敏感字段剥离、失败不覆盖和显式路径 CLI 测试。

### 验证

本地运行新增同步器定向测试、完整 unittest、scripts compileall、四个公开快照 JSON 语法检查和 `git diff --check`。

## 1.1.0 — 2026-09-10

兼容新增普通 ChatGPT Chat 读取方式，不改变 schema v6、人物 schema 3、本地反馈状态机、Word 授权门或 Codex 本地执行流程。

### 本次优化

- 新增 `CHATGPT.md`，只处理普通 ChatGPT Chat 的环境差异；本地 Codex 默认忽略，不复制第二套创作规则。
- 新增 `chat-context/` 公开安全快照骨架，分别承载人物、近期反馈、近期选题和共享抽象偏好的脱敏投影。
- 快照默认 `not_synced`，未同步时网页 Chat 回退到 `persona-template.json` 和当前会话，不伪造本地台账、脚本执行、哈希或验收结果。
- `web-access` 在普通 Chat 环境下允许由 ChatGPT 原生 Web Search 替代，但 10 万赞原作、内容可读性和其他研究硬门不降低。
- README、AGENTS 与版本维护规则增加公开快照边界：原始业务内容、完整台账、正式交付和运行产物仍不进入 Public 仓库。

### 验证

本次未修改 Python、schema 或生成器逻辑。公开快照 JSON 骨架已做语法检查；ChatGPT 适配文档按新写、标题、局部修改、联网研究、本地脚本不可执行和正式 Word 六类路径进行静态审读。

当前 ChatGPT GitHub 集成只有读取权限，无法直接在远端提交；因此本条仅作为待本地 Codex 应用后的 1.1.0 发布记录。正式发布前仍需本地运行仓库既定 unittest、compileall、git diff --check，并由本地 Git 创建/推送 `boss-ip-v1.1.0` 标签。

## 1.0.0 — 2026-09-09

首次独立语义版本基线。候选schema v6、人物schema 3继续保留，历史v5/v4兼容读取；人物profile_revision更新为2，旧候选须使用迁移前人物快照复验。

### 本次优化

- 增加新写、参考续写、局部修改、标题和发布介绍入口；抖音、视频号按用户指定处理，纯包装不重跑新选题研究。
- 标题与首两句联审兑现，观点解释遗漏因素，对白回应现实阻力，结尾集中一个主要落点；高赞研究写清适配条件与实际采用位置。
- 人物模板统一为persona-template.json，初始化器从单一模板加载；迁移器保存旧字节快照、追加观点版本、更新现役行为及时长，删除重复legacy块及失效行为。普通init不覆盖已有实例。
- 修复用户指定数量时批次CLI、完整性校验和反馈投影仍写死三条的问题；非三条逐项统计并与三条批次标准隔离。
- 新增版本与清理规则、范围化Git提交约定，修正Word编号登记路径及项目交付入口链接。
- 仓库当前树移出历史业务人物实例与两份台账，归回业务项目留档；旧测试改用无业务身份信息的稳定夹具。共享偏好自身测试独立运行，跨Skill联调仅在另一个Skill已安装时运行。

### 纳入首个版本的已有改动

本版同时纳入版本编号前已在本地使用的10万赞原作研究门、标题字段与公开展示、口播主题对齐、批内语义差异审读、共享抽象创作偏好及其测试。它们不是本次新创功能；此前未提交状态已在业务项目留存基线。

### 验证

新增人物迁移4项及指定数量3项定向测试通过；完整本地环境212项unittest全部通过，compileall与git diff --check退出0。独立目录运行212项，其中210项通过、2项因未安装商品Skill而跳过的跨Skill联调；Boss自身的共享偏好校验与CLI测试继续执行。

Skill版本字段、相对链接及安装Junction哈希核验通过。业务项目保存三条历史候选按旧快照复验、过期引用拒绝、1/2条批次完整性及旧稿/台账/交付保护哈希证据。官方quick_validate在本机因缺少PyYAML无法启动，未新增依赖；独立结构核验不冒称官方检查通过。

机器检查只证明完整性与兼容边界，不代表用户可拍评价或传播效果。仓库当前树已移除业务文件，已有Git历史未重写。
